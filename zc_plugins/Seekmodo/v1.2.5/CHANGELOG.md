# Seekmodo Zen Cart connector — v1.2.5 changelog

## v1.2.5 — mobile split-rail slider fix (2026-06-23)

- **Suggest bundle** — fixes mobile split-rail draggable divider: the keyword
  rail `max-height` cap now applies only to the static mobile stack, not the
  resize mode, so dragging the divider expands/contracts both panels again.
  Default rail height remains ~28% with scroll for overflow.
