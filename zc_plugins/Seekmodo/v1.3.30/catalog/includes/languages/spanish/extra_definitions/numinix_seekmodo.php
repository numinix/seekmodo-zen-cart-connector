<?php
/**
 * Seekmodo language definitions (spanish).
 *
 * Zen Cart 1.5.7-compatible define() file. Loaded from
 * includes/languages/spanish/extra_definitions/ when that language is active.
 * Merchants may edit the string values; leave the constant names unchanged.
 */

if (!defined('TEXT_SEEKMODO_SUGGEST_RECENT')) {
    define('TEXT_SEEKMODO_SUGGEST_RECENT', 'Buscadas recientemente');
}
if (!defined('TEXT_SEEKMODO_SUGGEST_TRENDING')) {
    define('TEXT_SEEKMODO_SUGGEST_TRENDING', 'Tendencias');
}
if (!defined('TEXT_SEEKMODO_SUGGEST_KEYWORDS')) {
    define('TEXT_SEEKMODO_SUGGEST_KEYWORDS', 'Sugerencias');
}
if (!defined('TEXT_SEEKMODO_SUGGEST_PRODUCTS')) {
    define('TEXT_SEEKMODO_SUGGEST_PRODUCTS', 'Productos');
}
if (!defined('TEXT_SEEKMODO_SUGGEST_CATEGORIES')) {
    define('TEXT_SEEKMODO_SUGGEST_CATEGORIES', 'Categorías');
}
if (!defined('TEXT_SEEKMODO_SUGGEST_DID_YOU_MEAN')) {
    define('TEXT_SEEKMODO_SUGGEST_DID_YOU_MEAN', 'Quiso decir');
}
if (!defined('TEXT_SEEKMODO_SUGGEST_VIEW_ALL')) {
    define('TEXT_SEEKMODO_SUGGEST_VIEW_ALL', 'Ver los {total} resultados');
}
if (!defined('TEXT_SEEKMODO_SUGGEST_EMPTY')) {
    define('TEXT_SEEKMODO_SUGGEST_EMPTY', 'Aún no hay coincidencias — sigue escribiendo.');
}
if (!defined('TEXT_SEEKMODO_SUGGEST_POWERED_BY')) {
    define('TEXT_SEEKMODO_SUGGEST_POWERED_BY', 'Con tecnología de ');
}
if (!defined('TEXT_SEEKMODO_CORS_BLOCKED')) {
    define('TEXT_SEEKMODO_CORS_BLOCKED', 'Las sugerencias de búsqueda no se pudieron cargar porque este sitio no puede alcanzar Seekmodo (CORS). Pida al administrador de la tienda que autorice este dominio en la puerta de enlace de Seekmodo, o active el proxy de sugerencias same-origin del conector.');
}
