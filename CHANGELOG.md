# Seekmodo for Zen Cart - top-level changelog

## v1.3.82 - 2026-09-03 (Unified Suggest UI on EN / over-quota)

- Stop forcing legacy typeahead when unpaid/over_quota sticky.
- Keep `<seekmodo-suggest>` with `prefer-local` + same-origin EN fill.
- Enrich EN local rows (`image_url`) and emit `rows` from suggest shim.
- Vendor `@seekmodo/web-components` 0.4.7.

